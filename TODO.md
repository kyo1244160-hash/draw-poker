# TODO.md — タスク・優先順位・既知の問題

最終更新: 2026-04-17 (Vol.5)

---

## 🔴 優先度: 高（バグ・本番影響あり）

### テーブル移動後の最初のハンドに参加できない
- `t:tableTransfer`（3秒遅延）到着前にゲームが始まった場合、初手がタイムアウトになる
- `t:tableJoin` で `tableIdRef` は即更新済みなので betAction は正しいテーブルに送れる
- しかし waitZone（RRoP Rule 16）で `sittingOut=true` になっていると `startGame` がスキップする可能性
- **調査ポイント**: テーブル移動後の1手目タイムアウト時のログ `[betAction-debug]` を確認

### レイズ/チェックボタンが効かない（稀に残存）
- `[betAction-debug]` ログで `idMatch=false` か確認
- fix-v11/v20 で対策済みだが `tableIdRef` の更新が間に合わないケースが残る可能性
- **対策**: リロードで解消するなら `t:tableJoin` の適用が遅れているケース

---

## 🟡 優先度: 中（UX 改善・機能追加）

### ブラインドアップ表示の最終確認
- fix-v24b（`_io=false` 修正）で根本解決済みのはず
- 再現したら `[blind-debug]` ログを確認

### ゲームが始まらない（稀に残存）
- fix-v24b（多段リトライ + `_io` 修正）で大幅改善
- `[stuck-debug]` ログで `canAutoStart=false` の原因を確認

### Sit & Go
- [x] 基本実装完了
- [x] BOT を参加人数に含めて判定
- [x] 終了後に次の SNG を自動作成
- [ ] `max_players` に達したら即時開始
- [ ] SNG 待機中の「あと N 人」表示をリアルタイム更新（現状: 30秒ポーリング）

---

## 🟢 優先度: 低（将来対応）

- [ ] モバイル縦持ちレイアウト改善
- [ ] ファイナルテーブル到達時の演出強化
- [ ] 管理画面に「テスト開始ボタン」
- [ ] チャット機能（テーブル内）
- [ ] ハンド履歴・統計表示

---

## 既知の問題（即修正不要）

| 問題 | 状況 | 回避策 |
|---|---|---|
| iOS Safari でターン音が初回のみ鳴らない | AudioContext の初期化タイミング | 画面タップ後は正常 |
| Render コールドスタート後の接続が遅い | 無料プランのスリープ | useKeepalive でスリープ防止中 |
| `tournament_seats` テーブルが未使用 | ドロー画面のシート表示実装前に作成 | 特になし |

---

## 完了済みタスク（Vol.5 / 2026-04-17）

- [x] バランシング後 socket.id 不一致でレイズが効かない → fix-v11
- [x] ブラインド情報がハンド開始時に同期されない → fix-v12
- [x] 脱落・優勝オーバーレイに X シェア機能追加 → fix-v13/v14
- [x] テーブル移動が速すぎて showdown 確認できない → fix-v15（3秒遅延）
- [x] rank=0 のままシェア画像が「0位」になる → fix-v16
- [x] BOT が waitZone になりゲームが始まらない → fix-v19
- [x] テーブル移動後のアクションが旧テーブルに送られる → fix-v20（t:tableJoin 追加）
- [x] 再接続後にブラインド表示が更新されない → fix-v21（t:getMyTable 使用）
- [x] 複数レベル連続期限切れで表示が残り続ける → fix-v23（ループ適用）
- [x] `_kickstartAfterBalance` が1回しか試行しない → fix-v23b（多段リトライ）
- [x] `_io=false` で kickstart・ブラインド更新が全発火しない → fix-v24b（init() undefined 保護）

## 完了済みタスク（Vol.4 / 2026-04-13）

- [x] チェック可能時のフォールド禁止
- [x] フォールド拒否→チェックフォールバック（タイムアウト時）
- [x] SNG lateRegOpen フォールバック修正（Vol.4）
- [x] 通常トーナメント（レベルベース）開始直後に参加ボタンが表示されない → fix-v25
- [x] _disconnectedChips Map でチップ保持
- [x] SNG 二重作成防止（_sngRecreated フラグ）
- [x] 優勝エフェクト未表示バグ修正
- [x] テーブル移動で sittingOut 解除
